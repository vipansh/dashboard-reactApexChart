import { Chip } from "<components>/shared/Chip";
import React from "react";

const FilterModal = () => {
  return (
    <div>
      <Chip>FilterModal</Chip>FilterModal
    </div>
  );
};

export default FilterModal;
